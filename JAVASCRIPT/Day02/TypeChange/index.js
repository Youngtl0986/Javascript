const a = 1;
const b = "1";

const a1 = Number("100") + 20;
const a2 = Number("1004") + Number(10);

const b1 = String(100) + "10";
const b2 = String(13) + "14";

window.console.log(`결과:${a1}`);
window.console.log(`결과:${a2}`);
window.console.log(`결과:${b1}`);
window.console.log(`결과:${b2}`);
