const number1 = window.prompt("첫번째 숫자를 입력해주세요.");
const number2 = window.prompt("두번째 숫자를 입력해주세요.");

const sum1 = Number(number1) + Number(number2);

window.alert(`${number1} + ${number2} = ${sum1}`);

const age1 = window.prompt("나이를 입력해주세요.");
const sum2 = Number(2026) - Number(age1);

window.alert(`귀하의 태어난 년도는 ${sum2} 입니다!.`);
